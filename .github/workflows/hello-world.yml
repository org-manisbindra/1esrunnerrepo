name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: [ self-hosted, 1ES.Pool=1esghrunner-crt-via-portal ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, World!"

      # echo ip address of runner
      - name: Echo IP Address
        run: echo "IP Address $(ip a)"